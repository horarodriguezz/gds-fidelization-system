---
import { House, ServerCrash } from "@lucide/astro";
import { Button } from "../components/ui/button";
---

<div class='flex min-h-screen items-center justify-center bg-background p-4'>
  <div class='w-full max-w-md text-center'>
    <div class='relative mb-8 inline-block'>
      <div
        class='text-[120px] font-bold leading-none text-primary/20 sm:text-[140px]'
      >
        500
      </div>
      <div class='absolute inset-0 flex items-center justify-center'>
        <ServerCrash class='h-16 w-16 text-destructive sm:h-20 sm:w-20' />
      </div>
    </div>

    <h1 class='mb-3 text-2xl font-bold text-foreground sm:text-3xl'>
      Error del Servidor
    </h1>
    <p class='mb-8 text-pretty text-muted-foreground'>
      Lo sentimos, algo salió mal en nuestro servidor. Nuestro equipo ya fue
      notificado y está trabajando para resolver el problema.
    </p>

    <div class='flex flex-col gap-3 sm:flex-row sm:justify-center'>
      <a href='/'>
        <Button variant='outline' size='lg' className='gap-2'>
          <House class='h-4 w-4' />
          Ir al Inicio
        </Button>
      </a>
    </div>
  </div>
</div>
